6502 assembler written in machine code, as Lord intended.

## How to run it?

Go to `stage0` and run `assemble.py` script. It will produce `assembler.bin`,
`input.asm` will be embedded in the file, `main` function is located at `0x800`.
